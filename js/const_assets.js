// 画像ファイルのディレクトリ
const DIR_CONST = {
    DIR_ASSETS: "./assets",
    DIR_IMG: DIR_CONST.DIR_ASSETS + "/img"
}

// 各画像ファイルのファイル名
const IMGNAME_CONST = {
    IMG_REV_ALL: "PuzzleUnit_All.png"
}

// 各画像ファイルのファイルID
const IMG_CONST = {
    IMG_REV_ALL: "PuzzleUnitAll"
}